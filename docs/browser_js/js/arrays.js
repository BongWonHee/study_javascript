let init_array = new Array(); // [] -> Array의 초기화 방법

let fruits = ["apple", "banana", "orange", "mellon"];

// fruits.length // 내부의 정보의 갯수 확인

// fruits.push('grape'); //값을 추가

// fruits.indexOf('banana'); //해당 문자의 위치값 확인

// fruits.join(' and ') // 인덱스 값사이의 특정 글자나 문자를 넣을때 사용함

let fruits_second = ["berry", "strawberry"];

let fruitsConcat = fruits.concat(fruits_second); // 두개의 리스트를 합쳐줄때
